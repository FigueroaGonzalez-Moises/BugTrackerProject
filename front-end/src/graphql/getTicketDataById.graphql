query getTicketDataById($id: String!) {
    getTicketDataById(id: $id) {
        ticketid
        priority
        title
        description
        type
        submitter
        developer
        status
        belongsto
    }
}